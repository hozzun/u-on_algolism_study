# 시험감독
import sys
input = sys.stdin.readline

N = int(input())
arr = list(map(int, input().split()))
b, c = map(int, input().split())

cnt = 0
for i in arr:
    i = i - b
    cnt += 1

    if i > 0:
        while i > 0:
            i = i - c
            cnt += 1

print(cnt)
